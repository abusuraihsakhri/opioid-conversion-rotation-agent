# Opioid Conversion Rotation Agent - Environment Configuration
# Copy this file to .env and fill in your values

# Secret key for HMAC-SHA256 audit trail (required for audit features)
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
AUDIT_SECRET_KEY=your-secure-audit-key-here-minimum-16-chars
